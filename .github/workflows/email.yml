name: Hello World

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 24f2007572@ds.study.iitm.ac.in
        run: echo "Hello, world!"
